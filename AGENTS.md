# Agent instructions

Este repositorio se desarrolla con la especificación en `/docs`.

1. Lee `docs/README.md` y `docs/CURSOR.md` antes de cualquier cambio de código.
2. La fase activa está en `docs/15-ROADMAP.md`. No adelantes fases.
3. Orden: modelo → API → auth → marketplace → web → mobile.
4. Prisma es la única fuente de esquema. RBAC obligatorio. Sin `any`.
5. No scrapees ni copies TCGMatch.

Detalle normativo: [docs/CURSOR.md](docs/CURSOR.md).
