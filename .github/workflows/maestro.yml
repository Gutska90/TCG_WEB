name: Maestro (optional)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Not a CI gate
        run: |
          echo "Maestro no corre en el job check: GitHub ubuntu-latest no trae emulador Android/iOS."
          echo "Local o runner con simulador: pnpm test:maestro"
          ls apps/mobile/.maestro
